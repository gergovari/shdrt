#pragma once

/**
 * @typedef shdrt_ServiceBinder
 * @brief Opaque handle to a service binder.
 * 
 * Binders are used for inter-process communication (IPC) between components.
 * The exact structure is implementation-defined.
 */
typedef void shdrt_ServiceBinder;
